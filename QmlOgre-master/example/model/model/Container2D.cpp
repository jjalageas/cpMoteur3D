#include "Container2D.h"
#include <iostream>
/*
using namespace std;

Container2D::Container2D(Image3D image3D)
{

  int i=0;

  for(i=0;i<image3D.getWidth();i++)
    sagital.push_back(image3D.getSlice(i,SliceType.SAGITAL));

  for(i=0;i<image3D.getDepth();i++)
    sagital.push_back(image3D.getSlice(i,SliceType.FRONTAL));

  for(i=0;i<image3D.getHeight();i++)
    sagital.push_back(image3D.getSlice(i,SliceType.CORONAL));
}


Container2D::~Container2D()
{

}

 Image<float> getCoupe(int i, SliceType type)
 {

   switch(type) {
   case CORONAL:
     return coronal[i];
   case FRONTAL:
     return frontal[i];
   case SAGITAL:
     return sagital[i];
   default:
     eassert(false);
     return NULL;
   }

 }
*/
